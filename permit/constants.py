DEFAULT_PDP_URL = "http://localhost:7000"
DEFAULT_TENANT_KEY = "default"

# Objects Names
OBJECT_ENVIRONMENT_NAME = "environment"
OBJECT_PROJECT_NAME = "project"
OBJECT_TENANT_NAME = "tenant"

# Log messages
DEPRECATION_WARNING_LOG = (
    "DEPRECATED ! Please use the API level method - instead of `{}` use `{}`."
)
